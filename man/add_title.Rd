% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{add_title}
\alias{add_title}
\title{Add a title above a plot}
\usage{
add_title(
  plot,
  title,
  heights = c(1, 9),
  family = getOption("ggshort.font"),
  size = 14,
  hjust = NULL,
  vjust = NULL
)
}
\arguments{
\item{plot}{A ggplot object or gtable.}

\item{title}{Character string for the title text.}

\item{heights}{Numeric vector of relative heights for title vs plot
(default \code{c(1, 9)}).}

\item{family}{Font family for the title (default \code{getOption("ggshort.font")}).}

\item{size}{Font size for the title (default \code{14}).}

\item{hjust, vjust}{Numeric justification values passed to \code{\link[grid:grid.text]{grid::textGrob()}}.}
}
\value{
A gtable object with the title stacked on top. Use \code{\link[grid:grid.draw]{grid::grid.draw()}}
to render it.
}
\description{
Place a title-like text grob above a ggplot or gtable, stacking it
vertically with the main plot.
}
\examples{
\donttest{
p <- ggbar(mtcars, x = factor(cyl), y = mpg) + theme_view()
add_title(p, "MPG by cylinder")
}

}
