% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample.R
\name{plot_sample_size}
\alias{plot_sample_size}
\title{Plot sampling distributions of estimated proportions}
\usage{
plot_sample_size(
  x,
  times = 5000,
  fmt = function(x) sprintf("\%.2f", x * 100),
  nrow = NULL,
  ncol = NULL,
  scales = "fixed",
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{A \code{sample_size} object created by \code{compute_sample_size(as_dt = TRUE)}.}

\item{times}{Integer; number of simulated samples per parameter combination (default: \code{5000}).}

\item{fmt}{Function applied to x-axis labels (default: percentage formatter).}

\item{nrow}{Integer; number of rows for facet layout (default: automatic).}

\item{ncol}{Integer; number of columns for facet layout (default: automatic).}

\item{scales}{Character; facet scaling, one of \code{"fixed"}, \code{"free"}, \code{"free_x"}, or \code{"free_y"}.}

\item{theme}{One of \code{"view"}, \code{"save"}, or \code{"shiny"}, passed to \code{\link[=switch_theme]{switch_theme()}}.}

\item{...}{Additional arguments forwarded to \code{\link[=switch_theme]{switch_theme()}}.}
}
\value{
A \code{ggplot} object showing simulated sampling distributions
for all combinations of \code{p}, \code{r}, and \code{conf.level}.
}
\description{
Generates a faceted histogram and density plot of simulated sampling
distributions for proportion estimates (\code{phat}), based on a
\code{sample_size} design object.
}
\details{
Each facet corresponds to a unique parameter combination, labelled
by \code{p}, \code{r}, \code{conf.level}, and \code{n}.

The x-axis shows simulated proportion estimates (\code{phat}), and the
y-axis shows estimated density. This visualization is useful for
understanding how precision and uncertainty change with sample size.
}
\examples{
\donttest{
ss <- instead::compute_sample_size(p = c(0.03, 0.05), r = c(0.1, 0.2),
                                   conf.level = 0.95, as_dt = TRUE)
plot_sample_size(ss, times = 3000)
}

}
\seealso{
\code{\link[instead:compute_sample_size]{instead::compute_sample_size()}}, \code{\link[instead:simulate_sample_size]{instead::simulate_sample_size()}}
}
