% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stack.R
\name{stack_plots}
\alias{stack_plots}
\alias{vstack_plots}
\alias{hstack_plots}
\title{Stack/arrange two plots (vertical or horizontal)}
\usage{
vstack_plots(top, bottom, heights = c(1, 1), draw = FALSE)

hstack_plots(left, right, widths = c(1, 1), draw = FALSE)
}
\arguments{
\item{top, bottom}{Plots to stack vertically (for \code{vstack_plots()}).}

\item{heights}{Numeric vector of two relative heights (default \code{c(1, 1)}).
Used by \code{vstack_plots()}.}

\item{draw}{Logical; if \code{TRUE}, draw the combined gtable immediately.
Default \code{FALSE}.}

\item{left, right}{Plots to arrange horizontally (for \code{hstack_plots()}).}

\item{widths}{Numeric vector of two relative widths (default \code{c(1, 1)}).
Used by \code{hstack_plots()}.}
}
\value{
A gtable object representing the combined layout (invisible).
}
\description{
Combine two plotting objects into a single gtable layout:
\itemize{
\item \code{vstack_plots()} places one plot \strong{above} the other (rows).
\item \code{hstack_plots()} places plots \strong{side by side} (columns).
}
}
\details{
Inputs may be ggplot, gtable, or grid grob objects; they are coerced
internally to a gtable. Relative sizes are controlled by \code{heights} (vertical)
or \code{widths} (horizontal). When \code{draw = TRUE}, the result is drawn immediately.
}
\examples{
\donttest{
set.seed(123)
df <- expand.grid(x = c("A","B","C"), fill = c("X","Y","Z"))
df$y <- sample(1:10, 9, TRUE)
p1 <- ggbar(df, x = x, y = y, fill = fill) + theme_view()

df2 <- expand.grid(x = 1:20, color = c("X","Y","Z"))
df2$y <- sample(1:10, nrow(df2), TRUE)
p2 <- ggline(df2, x = x, y = y, color = color) + theme_view()

# vertical stack
vstack_plots(p1, p2, heights = c(1, 1), draw = TRUE)

# horizontal stack
hstack_plots(p1, p2, widths = c(1, 1), draw = TRUE)
}
}
