% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/short.R
\name{ggdensity}
\alias{ggdensity}
\title{ggplot density helper (frequently used arguments)}
\usage{
ggdensity(
  data,
  x,
  color = NULL,
  fill = NULL,
  group = NULL,
  probs = 0.95,
  na.rm = TRUE,
  y = Inf,
  show_mean = FALSE,
  show_median = FALSE,
  show_vline = TRUE,
  show_label = TRUE,
  label_args = list(family = getOption("ggshort.font"), size = 4, angle = 90, hjust = 2,
    vjust = 0.5, color = "black")
)
}
\arguments{
\item{data}{A data.frame.}

\item{x}{Unquoted column mapped to the x aesthetic.}

\item{color, fill, group}{Optional columns mapped to the corresponding
aesthetics.}

\item{probs}{Numeric vector of probabilities in [0, 1] for quantile guides
(used when \code{show_vline}/\code{show_label} are \code{TRUE}). Default \code{0.95}.}

\item{na.rm}{Logical; remove missing values silently if \code{TRUE}. Default \code{TRUE}.}

\item{y}{Numeric y position for quantile labels (used by \code{show_label}).
Default \code{Inf} (top of panel).}

\item{show_mean, show_median}{Logical; add mean/median vertical guides
(via \code{stat_mean_vline()} / \code{stat_median_vline()}). Defaults \code{FALSE}.}

\item{show_vline}{Logical; draw vertical quantile line(s) at \code{probs}
(via \code{stat_density_quantile_vline()}). Default \code{FALSE}.}

\item{show_label}{Logical; add quantile label(s) at \code{probs}
(via \code{stat_density_quantile()}). Default \code{TRUE}.}

\item{label_args}{A named list of \code{geom_text()} style options.
Supported keys: \code{family}, \code{size}, \code{angle}, \code{hjust}, \code{vjust}, \code{color}.}
}
\value{
A ggplot object.
}
\description{
Quickly draw kernel density curves with common options. Supply \strong{unquoted}
column names for aesthetics. Supports optional mean/median guides and
quantile lines/labels.
}
\examples{
\donttest{
# Basic density by group with 50\% quantile line (median) and label
ggdensity(iris, x = Sepal.Length, color = Species, fill = Species,
          probs = 0.5, show_vline = TRUE, show_label = TRUE) +
  theme_view()
}

}
\seealso{
\code{\link[=ggbar]{ggbar()}}, \code{\link[=ggline]{ggline()}}, \code{\link[=ggpoint]{ggpoint()}}, \code{\link[=ggjitter]{ggjitter()}}, \code{\link[=ggscatter]{ggscatter()}},
\code{\link[=ggbox]{ggbox()}}, \code{\link[=ggpie]{ggpie()}}, \code{\link[=ggmix]{ggmix()}}, \code{\link[=ggtable]{ggtable()}},
\code{\link[=stat_density_quantile_vline]{stat_density_quantile_vline()}}, \code{\link[=stat_density_quantile]{stat_density_quantile()}}
}
