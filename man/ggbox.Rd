% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/short.R
\name{ggbox}
\alias{ggbox}
\title{ggplot boxplot helper (frequently used arguments)}
\usage{
ggbox(
  data,
  x,
  y,
  color = NULL,
  fill = NULL,
  group = NULL,
  text = NULL,
  label,
  label_args = list(family = getOption("ggshort.font"), size = 4, angle = 0, hjust = 0.5,
    vjust = 0.5, color = "black"),
  width = 0.6,
  varwidth = FALSE,
  show_point = TRUE,
  show_mean = TRUE
)
}
\arguments{
\item{data}{A data.frame.}

\item{x, y}{Unquoted column names mapped to the x and y aesthetics.}

\item{color, fill, group}{Optional columns mapped to the \code{color}, \code{fill} and
\code{group} aesthetics.}

\item{text}{Optional column/expression used for tooltip text (e.g., plotly).}

\item{label}{Optional unquoted column/expression used for text labels
(placed at the box middle by default).}

\item{label_args}{A named list of \code{geom_text()} style options.
Supported keys: \code{family}, \code{size}, \code{angle}, \code{hjust}, \code{vjust}, \code{color}.}

\item{width}{Box width passed to \code{\link[ggplot2:geom_boxplot]{ggplot2::geom_boxplot()}} (default \code{0.6}).}

\item{varwidth}{Logical; if \code{TRUE}, boxes are drawn with widths proportional
to the square-roots of the number of observations in the groups.}

\item{show_point}{Logical; if \code{TRUE}, overlay jittered points
(\code{\link[ggplot2:geom_jitter]{ggplot2::geom_jitter()}}) on top of the boxplots.}

\item{show_mean}{Logical; if \code{TRUE}, add mean markers
using \code{\link[=stat_mean_point]{stat_mean_point()}}}
}
\value{
A ggplot object.
}
\description{
Quickly draw boxplots with common options. Supply \strong{unquoted}
column names for aesthetics. Supports optional mean/median markers
and overlaid jittered points.
}
\examples{
\donttest{
# Basic grouped boxplot with jittered points
ggbox(iris, x = Species, y = Sepal.Length, fill = Species,
      show_point = TRUE) +
  theme_view()

# Add labels at the middle of each box (here, the group name)
ggbox(iris, x = Species, y = Sepal.Length, fill = Species,
      label = Species, label_args = list(vjust = -0.4)) +
  theme_view()
}

}
\seealso{
\code{\link[=ggbar]{ggbar()}}, \code{\link[=ggline]{ggline()}}, \code{\link[=ggpoint]{ggpoint()}}, \code{\link[=ggjitter]{ggjitter()}}, \code{\link[=ggscatter]{ggscatter()}},
\code{\link[=ggdensity]{ggdensity()}}, \code{\link[=ggpie]{ggpie()}}, \code{\link[=ggmix]{ggmix()}}, \code{\link[=ggtable]{ggtable()}}
}
