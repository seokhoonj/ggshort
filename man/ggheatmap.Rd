% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/short.R
\name{ggheatmap}
\alias{ggheatmap}
\title{ggplot heatmap helper}
\usage{
ggheatmap(
  data,
  x,
  y,
  text = NULL,
  label = NULL,
  label_args = list(family = getOption("ggshort.font"), size = 4, angle = 0, hjust = 0.5,
    vjust = 0.5, color = "black"),
  fill,
  fill_args = list(low = "white", high = "mistyrose", mid = "white", midpoint = NULL, na
    = "white", color = NA, linetype = "solid", linewidth = 0.3, alpha = 1, guide =
    "colourbar"),
  xlab_position = c("bottom", "top"),
  ylab_position = c("left", "right")
)
}
\arguments{
\item{data}{A data.frame.}

\item{x, y}{Unquoted columns or expressions mapped to the heatmap axes.
Character, numeric, and Date values are automatically converted to factors.}

\item{text}{Optional column/expression for tooltip text (for example, when
used with plotly).}

\item{label}{Optional unquoted column/expression used as text labels inside cells.}

\item{label_args}{A named list of \code{geom_text()} style options.
Supported keys: \code{family}, \code{size}, \code{angle}, \code{hjust}, \code{vjust}, \code{color}.}

\item{fill}{A required numeric column/expression used for continuous cell fill.}

\item{fill_args}{A named list controlling continuous fill and cell border style.
Supported keys:
\describe{
\item{low}{Low-end fill colour. Default is \code{"white"}.}
\item{high}{High-end fill colour. Default is \code{"mistyrose"}.}
\item{mid}{Midpoint fill colour used only when \code{midpoint} is not \code{NULL}.
Default is \code{"white"}.}
\item{midpoint}{Optional numeric midpoint for diverging fill. If \code{NULL}
(default), a sequential gradient is used.}
\item{na}{Fill colour for \code{NA} values. Default is \code{"white"}.}
\item{color}{Border colour for heatmap cells. Default is \code{NA}.}
\item{linetype}{Line type for cell borders. Default is \code{"solid"}.}
\item{linewidth}{Line width for cell borders. Default is \code{0.3}.}
\item{alpha}{Alpha transparency for cell fill. Default is \code{1}.}
\item{guide}{Legend guide. Default is \code{"colourbar"}. Use \code{"none"} to hide.}
}}

\item{xlab_position}{Position of x-axis labels, one of \code{"bottom"} (default)
or \code{"top"}.}

\item{ylab_position}{Position of y-axis labels, one of \code{"left"} (default)
or \code{"right"}.}
}
\value{
A ggplot object representing a heatmap-like layout.
}
\description{
Create a heatmap-like plot using \code{ggplot2}. Values mapped to \code{x} and \code{y}
are coerced to factors when possible, then converted to numeric positions
so that each cell can be drawn on a regular grid. Optional labels are placed
inside cells, and fill colours are mapped continuously from a numeric variable.

Unlike many other ggshort helpers, \code{x}, \code{y}, \code{label}, and \code{fill} are first
evaluated and stored as temporary columns. This allows \code{ggheatmap()} to accept
bare column names, quasiquoted symbols, \code{.data[[...]]} expressions, and
other expressions that evaluate to vectors of the same length as \code{data}.
}
\details{
\code{ggheatmap()} is designed for compact, matrix-like displays where continuous
fill is more important than table-style grid lines. It is especially useful
for heatmaps, development diagnostics, and triangle-style colour displays.

Axis values supplied to \code{x} and \code{y} are coerced to factors with sorted levels
(if they are character, numeric, or Date), then drawn as equally spaced cells.

If \code{fill_args$midpoint} is \code{NULL}, a sequential gradient is used via
\code{\link[ggplot2:scale_gradient]{ggplot2::scale_fill_gradient()}}. Otherwise, a diverging gradient is used via
\code{\link[ggplot2:scale_gradient]{ggplot2::scale_fill_gradient2()}}.

The y-axis is reversed so that table-like and triangle-like displays read from
top to bottom in the same way as \code{\link[=ggtable]{ggtable()}}.
}
\examples{
\donttest{
set.seed(123)

df <- expand.grid(
  x = paste0("C", 1:5),
  y = paste0("R", 1:5)
)
df$value <- rnorm(25)

ggheatmap(
  df,
  x = x,
  y = y,
  fill = value
) +
  theme_view()

ggheatmap(
  df,
  x = x,
  y = y,
  label = sprintf("\%.2f", value),
  fill = value,
  fill_args = list(
    low = "#d9ecff",
    mid = "white",
    high = "#f8d7da",
    midpoint = 0,
    color = "grey80",
    guide = "none"
  )
) +
  theme_view()

x_var <- "x"
y_var <- "y"
ggheatmap(
  df,
  x = .data[[x_var]],
  y = .data[[y_var]],
  fill = value
) +
  theme_view()
}

}
\seealso{
\code{\link[=ggbar]{ggbar()}}, \code{\link[=ggline]{ggline()}}, \code{\link[=ggpoint]{ggpoint()}}, \code{\link[=ggjitter]{ggjitter()}}, \code{\link[=ggscatter]{ggscatter()}},
\code{\link[=ggdensity]{ggdensity()}}, \code{\link[=gghistogram]{gghistogram()}}, \code{\link[=ggbox]{ggbox()}}, \code{\link[=ggpie]{ggpie()}}, \code{\link[=ggmix]{ggmix()}}, \code{\link[=ggtable]{ggtable()}}
}
