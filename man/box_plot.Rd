% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/box.R
\name{box_plot}
\alias{box_plot}
\title{Box plot with optional jitter, mean marker and side density}
\usage{
box_plot(
  data,
  x_var,
  color_var,
  alpha = 0.6,
  width = 0.6,
  varwidth = FALSE,
  flip = FALSE,
  show_point = TRUE,
  show_mean = TRUE,
  show_density = FALSE,
  show_label = FALSE,
  label_args = list(family = getOption("ggshort.font"), size = 4, angle = 0, hjust = 0.5,
    vjust = -0.25, color = "black"),
  palette = "Set1",
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{x_var}{Character. Numeric column name for the box plot value
(e.g., \code{"Sepal.Length"}).}

\item{color_var}{Character. Grouping/color column name (typically a factor;
e.g., \code{"Species"}).}

\item{alpha}{Numeric in [0,1]. Transparency for fills/points (used in
\code{geom_boxplot()}, jitter points, and the optional side density).
Default \code{0.6}.}

\item{width}{Numeric box width passed to \code{\link[ggplot2:geom_boxplot]{ggplot2::geom_boxplot()}} (default \code{0.6}).}

\item{varwidth}{Logical; if \code{TRUE}, draw boxes with widths proportional to
the square-root of group sample sizes.}

\item{flip}{Logical; if \code{TRUE}, flip coordinates to make a horizontal box plot.}

\item{show_point}{Logical; if \code{TRUE}, overlay jittered points on the boxes.}

\item{show_mean}{Logical; if \code{TRUE}, add mean markers using \code{\link[=stat_mean_point]{stat_mean_point()}}.
Two layers are added: a black stroked point and a red point grouped by \code{color_var}.}

\item{show_density}{Logical; if \code{TRUE}, attach a density plot of \code{x_var}
by \code{color_var} to the main plot (to the \strong{right}; to the \strong{top} when \code{flip = TRUE}).}

\item{show_label}{Logical; if \code{TRUE}, add text labels at each box's median
position using the \code{color_var} values.}

\item{label_args}{A named list of \code{geom_text()} style options for labels.
Supported names: \code{family}, \code{size}, \code{angle}, \code{hjust}, \code{vjust}, \code{color}.}

\item{palette}{Discrete palette name for categorical \code{color_var}, forwarded to
\code{\link[ggplot2:scale_brewer]{ggplot2::scale_color_brewer()}} and \code{\link[ggplot2:scale_brewer]{ggplot2::scale_fill_brewer()}}.
Default \code{"Set1"}.}

\item{title, subtitle, caption}{Optional text passed to plot labels (via your theme helper).}

\item{theme}{One of \code{"view"}, \code{"save"}, or \code{"shiny"}, forwarded to \code{\link[=switch_theme]{switch_theme()}}.}

\item{...}{Additional arguments forwarded to \code{\link[=switch_theme]{switch_theme()}}.}
}
\value{
A ggplot object.
}
\description{
Quickly draw grouped box plots with a consistent interface. You pass
\strong{column names as strings} for the value and group variables. The function
can overlay jittered points, add mean markers, and (optionally) attach a
density subplot to the \strong{right} (or to the \strong{top} when \code{flip = TRUE}).
}
\details{
When \code{show_density = TRUE}, the side density is created with minimal
styling (no axes/legend margins) and attached using an internal gtable
combiner. The result is wrapped back into a regular ggplot via
\code{\link[=grob_to_ggplot]{grob_to_ggplot()}}, so you can continue adding layers/themes as usual.
}
\examples{
\donttest{
# Basic grouped box plot with jittered points
box_plot(
  iris, x_var = "Sepal.Length", color_var = "Species",
  title = "Box plot example"
)

# Add labels at each box's median and hide mean markers
box_plot(
  iris, x_var = "Sepal.Length", color_var = "Species",
  show_mean = FALSE, show_label = TRUE,
  label_args = list(vjust = -0.4),
  title = "Box plot example"
)

# Horizontal box plot with density on top
box_plot(
  iris, x_var = "Sepal.Length", color_var = "Species",
  flip = TRUE, show_density = TRUE,
  title = "Box plot example"
)
}

}
\seealso{
\code{\link[=stat_mean_point]{stat_mean_point()}}, \code{\link[ggplot2:geom_boxplot]{ggplot2::geom_boxplot()}}, \code{\link[=grob_to_ggplot]{grob_to_ggplot()}},
\code{\link[=ggdensity]{ggdensity()}}, \code{\link[=switch_theme]{switch_theme()}}
}
