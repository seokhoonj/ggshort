% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{suppress_geom_removed_warnings}
\alias{suppress_geom_removed_warnings}
\title{Suppress "Removed rows" warnings in ggplot2}
\usage{
suppress_geom_removed_warnings(expr)
}
\arguments{
\item{expr}{An expression to evaluate, usually a ggplot2 plotting call
(e.g., \code{print(p)} or a \code{ggplot} expression).}
}
\value{
The result of evaluating \code{expr}. If \code{expr} itself returns
invisibly, the result will also be invisible.
}
\description{
Evaluates an expression while silencing the common ggplot2 warnings:
\itemize{
\item \code{"Removed n row(s) containing missing values"}
\item \code{"Removed n row(s) containing missing values or values outside the scale range"}
}
}
\details{
Such warnings are typically emitted by \verb{geom_*()} layers (e.g.,
\code{geom_point()}, \code{geom_bar()}, \code{geom_text()}) when data contain \code{NA}s
or values outside the axis limits. In exploratory plots or expected-data
scenarios, these warnings may be considered harmless.
}
\examples{
\dontrun{
df <- data.frame(x = c("A", "B", NA), y = c(1, 2, NA))

p <- ggbar(df, x = x, y = y)

# Normally produces warnings about removed rows
print(p)

# Suppress those warnings
suppress_geom_removed_warnings(print(p))
}

}
