% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{scale_x_break}
\alias{scale_x_break}
\alias{scale_y_break}
\title{Continuous Position Scale for X-Axis with Custom Breaks}
\usage{
scale_x_break(break_interval, ...)

scale_y_break(break_interval, ...)
}
\arguments{
\item{break_interval}{A numeric value specifying the interval between x-axis breaks.}

\item{...}{Other arguments passed to \code{\link[ggplot2:scale_continuous]{ggplot2::scale_x_continuous()}},
such as \code{limits}, \code{labels}, or \code{expand}.}
}
\value{
A ggplot2 scale object modifying the x-axis breaks.
}
\description{
\code{scale_x_break()} provides a continuous position scale for the x-axis,
allowing users to specify the interval between axis breaks.
It rounds the axis limits to align with the specified break interval.
}
\section{Computation}{

This function dynamically calculates axis breaks based on the range of x values in the data.
\itemize{
\item If \code{break_interval} is an integer, the axis breaks are rounded to the nearest whole numbers.
\item If \code{break_interval} is a decimal, the scale retains fractional values without rounding.
}
}

\examples{
library(ggplot2)

# Example using the iris dataset
ggplot(iris, aes(x = Sepal.Length, y = Sepal.Width)) +
  geom_point() +
  scale_x_break(break_interval = 0.5)

ggplot(iris, aes(x = Sepal.Length, y = Petal.Length)) +
  geom_point() +
  scale_x_break(break_interval = 1, limits = c(4, 8))

}
\seealso{
\code{\link[ggplot2:scale_continuous]{ggplot2::scale_x_continuous()}}
}
