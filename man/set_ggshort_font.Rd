% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/font.R
\name{set_ggshort_font}
\alias{set_ggshort_font}
\alias{get_ggshort_font}
\title{Set or get the default font for ggshort plots}
\usage{
set_ggshort_font(family)

get_ggshort_font()
}
\arguments{
\item{family}{A character string giving the font family to use.
\itemize{
\item Use \code{""} (empty string) to reset to system default.
\item Must match a font available in \code{\link[systemfonts:system_fonts]{systemfonts::system_fonts()}}.
}}
}
\value{
\itemize{
\item \code{set_ggshort_font()}: the font family (character scalar) that was set.
\item \code{get_ggshort_font()}: the current font family (character scalar).
}
}
\description{
These helpers manage the global font family used by \code{ggshort} themes
and label helpers, stored in the option \code{"ggshort.font"}.
}
\details{
\itemize{
\item \code{set_ggshort_font()} updates the option, verifying that the font exists
in \code{\link[systemfonts:system_fonts]{systemfonts::system_fonts()}}. If the font is not found, the previous
option value is restored and a warning is issued.
\item \code{get_ggshort_font()} retrieves the currently set option.
}
}
\examples{
\dontrun{
# Set Comic Sans MS if available
set_ggshort_font("Comic Sans MS")

# Reset to system default
set_ggshort_font("")

# Get current font
get_ggshort_font()
}

}
