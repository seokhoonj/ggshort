% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cor.R
\name{cor_plot}
\alias{cor_plot}
\title{Correlation plot}
\usage{
cor_plot(
  x,
  display = c("lower", "upper"),
  show_diag = FALSE,
  show_label = TRUE,
  label_args = list(family = getOption("ggshort.font"), size = 4, angle = 0, hjust = 0.5,
    vjust = 0.5, color = "black"),
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{A numeric correlation matrix (square, symmetric, values in \verb{[-1, 1]}).}

\item{display}{Which triangle to display: \code{"lower"} or \code{"upper"}. Diagonal
cells are controlled separately via \code{show_diag}.}

\item{show_diag}{Logical; if \code{TRUE}, keep diagonal cells. Default \code{FALSE}.}

\item{show_label}{Logical; if \code{TRUE}, print correlation values on tiles.
Default \code{TRUE}.}

\item{label_args}{A named list of text style options for \code{geom_text()}.
Supported keys: \code{family}, \code{size}, \code{angle}, \code{hjust}, \code{vjust}, \code{color}.}

\item{title, subtitle, caption}{Plot title, subtitle, and caption. Passed to
\code{\link[ggplot2:labs]{ggplot2::labs()}}.}

\item{theme}{A ggshort theme key used by \code{match_theme()}: one of
\code{"view"}, \code{"save"}, or \code{"shiny"}.}

\item{...}{Additional arguments forwarded to \code{match_theme(theme = ...)}
(e.g., \code{x.angle}, \code{x.hjust}).}
}
\value{
A ggplot object.
}
\description{
Draw a heatmap-style plot for a correlation matrix.
}
\examples{
\dontrun{
x <- cor(mtcars)
# lower triangle with labels
cor_plot(x, display = "lower")

# upper triangle without diagonal or labels
cor_plot(x, display = "upper", show_diag = FALSE, show_label = FALSE)
}

}
